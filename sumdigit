#include<stdio.h>
int main(){
    int num,sum=0,m;

    printf("enter the number\n");
    scanf("%d",&num);
    
    while(num!=0){
        m=num%10;
        sum=sum+m;
        num=num/10;
        
    }
    printf("sum is %d",sum);
    return 0;
}
